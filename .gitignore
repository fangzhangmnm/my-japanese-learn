# Design configs / curriculum docs stay LOCAL — never publish to the public Pages repo.
*.md
# …except the public repo landing page.
!README.md

# OS cruft
.DS_Store
Thumbs.db
*.tmp

# Per-learner runtime state (the downloadable source of truth) is not source content.
telemetry-*.json

# Local Claude Code config / worktrees — never publish.
.claude/
